// Switch for the recruitment waitlist form on /join. Sign-ups land in Supabase
// (`signups`) and the EB reads them in the portal under Submissions > Waitlist.

// Show the "notify me when registration opens" form.
export const WAITLIST_OPEN = true
