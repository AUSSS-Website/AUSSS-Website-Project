// Switch for exchange-story submissions (/exchange/share). Stories land in
// Supabase (`stories`); the exchange officers and the EB read them in the
// portal under Submissions > Stories.

// When false, /exchange/share shows a "submissions are paused" message
// instead of the form.
export const STORIES_OPEN = true
