# Copy to .env.local (gitignored). Vite exposes only VITE_* to the browser.
VITE_SUPABASE_URL=https://wjijkqrdaakiwbtdssio.supabase.co
# Publishable key (sb_publishable_...). Safe for the browser; RLS does the gatekeeping.
VITE_SUPABASE_ANON_KEY=
# Secret key (sb_secret_...). Bypasses RLS. ONLY for scripts/db/import-roster.mjs. Never VITE_.
SUPABASE_SECRET_KEY=
